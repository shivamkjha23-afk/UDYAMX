'use client';import { motion } from 'framer-motion';export function AnimatedCounter({value,label}:{value:string;label:string}){return <motion.div initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="glass rounded-2xl p-5"><div className="font-display text-3xl font-bold text-gradient">{value}</div><div className="mt-1 text-sm text-slate-300">{label}</div></motion.div>}
