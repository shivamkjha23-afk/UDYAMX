'use client';import Link from 'next/link';import { Command } from 'cmdk';import { Search } from 'lucide-react';import { useState } from 'react';import { nav } from '@/lib/data';
export function Navbar(){const[open,setOpen]=useState(false);return <><header className="fixed inset-x-0 top-0 z-50 border-b border-white/10 bg-[#030712]/75 backdrop-blur-xl"><nav className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/" className="font-display text-lg font-bold">NUMSMU <span className="text-electric">Labs</span></Link><div className="hidden gap-6 md:flex">{nav.map(([n,h])=><Link className="text-sm text-slate-300 hover:text-white" key={n} href={h}>{n}</Link>)}</div><button aria-label="Open command palette" onClick={()=>setOpen(true)} className="focus-ring rounded-full border border-white/15 p-2"><Search size={18}/></button></nav></header>{open&&<div className="fixed inset-0 z-[60] bg-black/70 p-4" onClick={()=>setOpen(false)}><Command onClick={e=>e.stopPropagation()} className="glass mx-auto mt-24 max-w-xl rounded-3xl p-4"><Command.Input className="w-full bg-transparent p-3 outline-none" placeholder="Search NUMSMU..."/><Command.List>{nav.map(([n,h])=><Command.Item key={n} onSelect={()=>location.href=h} className="cursor-pointer rounded-xl p-3 aria-selected:bg-white/10">{n}</Command.Item>)}</Command.List></Command></div>}</>}
export function Footer(){return <footer className="border-t border-white/10 px-4 py-12"><div className="mx-auto grid max-w-7xl gap-8 md:grid-cols-3"><div><b className="font-display">NUMSMU Labs</b><p className="mt-3 text-slate-400">Building India's innovation-to-industry ecosystem.</p></div><form className="glass rounded-2xl p-4"><label className="text-sm text-slate-300">Newsletter</label><div className="mt-3 flex gap-2"><input className="min-w-0 flex-1 rounded-full bg-white/10 px-4" placeholder="email@company.com"/><button className="rounded-full bg-electric px-4 text-navy font-semibold">Join</button></div></form><p className="text-sm text-slate-500">© 2026 NUMSMU Labs. Built for Vercel deployment.</p></div></footer>}
